# Public synthetic FinTS byte-syntax fixtures

`syntax-v1.json` contains six independently generated byte sequences and their
expected headers, positional fields, scalar kinds and decoded bytes. Wire data
uses base64 and scalar values use hex to make binary/control characters safe
to inspect. All examples are synthetic; none is a bank capture.

| Vector | Coverage |
| --- | --- |
| empty-structural-frame | Byte size, outer versions and matching message numbers |
| escaped-text-and-omissions | Five syntax escapes, absent/trailing fields and components, optional segment reference |
| binary-octets | All 256 octets, delimiter isolation and empty binary |
| response-reference-and-unknown-segment | Message reference, unknown version, non-ASCII and CR/LF bytes preserved without charset guessing |
| opaque-security-wrapper | Reserved outer numbering and an opaque binary payload |
| binary-component-and-empty-positions | Binary within a group, adjacent empty components and trailing omissions |

Regenerate from the repository root with
`python eng/Generate-FinTsVectors.py`. The optional producer uses Python's
standard library and no .NET code. Normal tests use the committed embedded
fixture and need no Python, PDF, network or bank service. Regeneration must
produce byte-identical fixture content.

Run `dotnet run --project tests/Broiler.Fond.Kernel.Tests`, or
`./eng/Invoke-CI.ps1 -Scope Full` for the full Windows checks.
`FinTsSyntaxTests` also verifies every frame prefix, malformed headers and
binary prefixes, data isolation, deterministic generated bytes, safe errors,
culture independence and fixed resource boundaries.

The empty structural frame and dummy wrapper are deliberately **not valid
banking dialogues or security-profile fixtures**. No fixture certifies BPD/UPD,
PIN/TAN, signatures, balance schemas, a bank or a supported operation. The
workflow simulator remains separate from this byte layer until typed segment
schemas can bridge them. Never substitute real account or credential data.

Reference details and exact scope: [ADR 0010](../../../../docs/adr/0010-fints-byte-syntax-and-framing.md).

## Typed response fixtures

`responses-v1.json` adds five independent response cases generated by the same
Python command: receipt, pending authorization/pagination, indeterminate
processing, an unknown code/body segment, and contradictory status. Expected
meanings are explicitly selected in the producer, separate from the .NET code.
Raw fields are compared byte-for-byte by `FinTsResponseTests`.

The response suite also checks schema limits, malformed/reference errors,
concurrent callers, per-instance replay protection, terminal stop and every
counter value through 9999. These are unwrapped synthetic schema frames;
they do not authenticate a bank or qualify a security procedure. The
correlation tracker sends nothing, and a match is not a completion result.
See [ADR 0011](../../../../docs/adr/0011-fints-responses-and-dialogue-correlation.md).

## Parameter fixtures

`parameters-v1.json` adds five synthetic BPD/UPD cases: general bank and listed
operations, unlisted-blocked policy, missing user scope, duplicate evidence,
and no accounts with an opaque endpoint segment. The existing Python generator
regenerates all three fixture files; .NET tests consume the embedded JSON.
They assert independently selected account/policy expectations and exact wire
retention. Additional tests cover typed fields, limits, cardinalities, omitted
sections and canonical extension positioning.

No account is allocated and no endpoint or parameter cache is updated. These
are parameter-evidence fixtures, not proof that an institution, user or operation
is authenticated or supported. [ADR 0012](../../../../docs/adr/0012-fints-parameter-evidence.md)
records supported versions, bounds and deliberately unsupported layouts.

## Read-capability fixtures

`read-capabilities-v1.json` adds five independent examples: matching balance
evidence, duplicate bank advertisements, an unsupported requested version with
no fallback, a disallowed single-account query, and an indeterminate response.
Expected issue sets are selected independently in the Python generator.
`FinTsReadCapabilityTests` also covers all six implemented schema versions,
format/advertisement bounds, context and signature conflicts, and cancellation.
No schema identifier is fetched and no operation is enabled by a matching result.
See [ADR 0013](../../../../docs/adr/0013-read-capability-evidence.md).

## PIN/TAN envelope and parameter fixtures

`pin-tan-v1.json` adds five independent response wrappers: PIN:1 with J,
PIN:2 with N and explicit zero bounds, omitted bounds with an unlisted
operation, duplicate conflicting operation flags, and an unknown HIPINS
version alongside known evidence. Each includes an opaque binary field
containing all 256 byte values inside the plaintext body. Expected profile,
advertisement count, operation evidence and exact inner bytes are independently
selected in the Python producer.

The .NET suite adds malformed and nested-wrapper rejection, exact combined
element/segment bounds, inner-reference correlation, duplicate/future
advertisements, optional fields, cancellation and safe diagnostics. No fixture
contains actual credentials, verifies authentication or qualifies a bank.
Reported N flags cannot establish SCA exemption. The local HIPINS limit is
125 operation pairs, below the protocol's 999 because of the syntax budget.
See [ADR 0014](../../../../docs/adr/0014-pin-tan-envelope-and-parameters.md).

## TAN procedure and challenge fixtures

`tan-schemas-v1.json` adds six independent cases: version-6 TAN/hash evidence,
version-7 separate-device polling, push approval, a dummy reference, duplicate
procedure/challenge evidence and future versions. Expectations include explicit
versions/counts, process codes, duplicate observations and opaque binary data.
The generator uses synthetic text, references and bytes only.

The .NET suite covers conditional fields, fixed diagnostics, cancellation,
wrapped response provenance, literal markup and exact/over-limit resources.
It parses no TAN credential, renders no challenge, schedules no polling and
does not treat dummy references as an SCA exemption. Local limits permit 12
version-6 or 9 version-7 procedures per advertisement, 128 advertisements,
1,024 total procedures, 32 challenges and 64 KiB of opaque HHD data per challenge.
See [ADR 0015](../../../../docs/adr/0015-tan-procedure-and-challenge-schemas.md).

## Permitted-procedure and challenge-context fixtures

`tan-context-v1.json` adds seven independent request/response pairs: an initial
challenge, a scoped exemption report with dummy fields, a hash mismatch,
pending separate-device approval, a status order-reference mismatch, an
unlisted procedure and an aborted dialogue that still reports methods.
Expected issue sets and outcome observations are selected in the Python
producer. All requests are unsigned synthetic frames with no credentials.

The .NET suite additionally exercises request/dialogue/version/profile scope,
duplicates, exact medium comparison, unsupported options, expiry review,
status placement, local bounds, cancellation and fixed diagnostics. A clean
observation neither authenticates a bank nor authorizes an operation. Pure
comparison intentionally does not consume a response or prevent replay.
See [ADR 0016](../../../../docs/adr/0016-permitted-procedures-and-challenge-context.md).

## SCA continuation traces

`sca-continuation-v1.json` adds eight independent traces. Each contains public
synthetic initial/query request-response pairs and explicitly chosen event,
time, transition and state expectations. Cases cover a single user handoff,
pending/execution observations, replay, timeout, cancellation in flight,
bank query exhaustion, push waiting, an exemption and a mismatched response.

The .NET suite executes the traces with a fake monotonic clock, then checks
context identity, policy limits, trigger rules, counter exhaustion, clock
regression and concurrent calls. No test sleeps, sends a query or accepts a
credential. Local handoff and terminal execution/exemption states are recorded
observations, not authorization or authenticated completion. Replay protection
is per model instance. See [ADR 0017](../../../../docs/adr/0017-in-memory-sca-continuation.md).

## Account-discovery and balance fixtures

`read-data-v1.json` adds eight independent cases: discovery of SEPA/non-SEPA
accounts, duplicate identifiers, empty discovery, uninterpreted discovery
versions 2/3, balance versions 6/7/8 and a debit-zero/currency-conflict report.
Each includes public unsigned request/response frames and explicitly chosen
expectations for account counts, exact signed amounts, absent available values,
escaped product labels and conflicting currencies. Identifiers are deliberately
synthetic; shape parsing does not imply valid bank identifiers or ownership.

The .NET suite also covers optional suffixes, malformed fields, canonical
amounts, calendar boundaries, account forms, resource ceilings, cancellation,
error/partial source status and fixed diagnostics. No request is sent and no
account or balance enters the domain. See
[ADR 0018](../../../../docs/adr/0018-account-discovery-and-balance-schemas.md).

## Read-context fixtures

`read-context-v1.json` adds eight independent cases with parameter, request and
response frames plus explicitly chosen issue sets, outcomes and tokens: matching
balance and selected discovery, wrong account/reference/version, partial data,
unsolicited continuation and an unsupported response schema.

The .NET suite additionally covers capability/account/dialogue scope, national
and entry-count permissions, duplicate/missing reports, currency conflicts,
status ambiguity, wrapped responses, independent counters, cancellation and
bounded page provenance. Comparisons consume no replay state or credentials.
See [ADR 0019](../../../../docs/adr/0019-read-request-response-context.md).

## Read-refresh lifecycle traces

`read-refresh-v1.json` adds eight independent traces with public parameter,
request and response frames, local page limits, event times and explicitly
selected transition/state expectations. They cover final execution, partial
continuation and replay, timeout, cancellation, page exhaustion, unrelated
references, bound account mismatch and stop before start.

The .NET suite additionally checks pinned context, deadline boundaries during
validation, reference release, exact page/counter limits and concurrent calls.
It uses fake monotonic clocks without sleeps or bank operations. Replay
rejection is per attempt; no page enters the domain or persistent storage.
See [ADR 0020](../../../../docs/adr/0020-in-memory-read-refresh-attempt.md).

## Read-availability fixtures

`read-availability-v1.json` adds eight independent cases for scoped unavailable
reports: empty/absent discovery, absent balance data, a conflicting zero balance,
missing reports without 3010, 9210 errors and partial/unavailable conflicts.
The Python producer explicitly selects expected issue sets, outcomes and source
report counts. Free-form text is synthetic and never interpreted as an account
state. The .NET suite also covers scope, ambiguity, continuation, timeout and
one-time terminal handling. See
[ADR 0021](../../../../docs/adr/0021-scoped-read-unavailability.md).

## All-account discovery fixtures

`all-discovery-v1.json` adds eight independent cases with parameter, request and
response frames. Explicit expectations cover each returned row's candidate count
and issues, unmatched UPD count and overall outcome: known SEPA/non-SEPA accounts,
unknown accounts, duplicate returned or UPD identities, conflicting national/IBAN
candidates, omitted known accounts, missing permission and unavailable results.

The .NET suite also verifies 512 distinct clean matches, 999 unknown returned
accounts, the 4096 candidate-link budget, source identity/order, status and
reference boundaries, cancellation and selected-account API restrictions.
See [ADR 0022](../../../../docs/adr/0022-all-account-discovery-evidence.md).

## All-account discovery attempt traces

`all-discovery-attempt-v1.json` adds eight independent traces with parameter,
request and response frames, event times and explicitly selected transition,
state and evidence-presence expectations. Cases cover one-time execution,
unavailability, ambiguity, cancellation, absolute timeout, foreign references,
stop before start and unsupported partial results.

The .NET suite also checks pinned inputs, caller-owned evidence after terminal
reference release, candidate-budget failures, deadline expiry during comparison,
clock regression and concurrent start/response/cancellation calls. Fake clocks
require no sleeps or bank operations. See
[ADR 0023](../../../../docs/adr/0023-in-memory-all-account-discovery-attempt.md).

## Unsigned read-request encoding fixtures

`unsigned-read-requests-v1.json` adds ten independent fixtures with typed caller
inputs and exact expected wire bytes. Explicit field layouts cover all-account
and selected discovery, duplicates, HKSAL versions 6/7/8, national/international
and combined tuples, optional pagination positions, escaped delimiters, Latin-1
text, field limits and boundary message counters. The Python producer never
uses the .NET writer's output.

The .NET suite compares bytes before parsing and adds text-range, invalid-input,
repetition-limit, culture, cancellation and lifecycle-integration checks. The
unsigned frames contain no credentials or security envelope. See
[ADR 0024](../../../../docs/adr/0024-unsigned-read-request-encoding.md).

## Initialization schema and encoding fixtures

`initialization-v1.json` adds eight independent input/wire pairs for HKIDN-2 and
HKVVB-3 in a restricted unsigned first-message frame. They cover first/cached
anonymous requests, identified system-status variants, all language codes,
escaped Latin-1 characters, internal spaces and maximum field lengths. The
anonymous marker has ten nines; the PDF's adjacent footnote is excluded.

The .NET suite verifies exact bytes and source ownership, then checks malformed
fields, binary substitutions, anonymous conflicts, Unicode and padded text,
frame/context restrictions, culture independence and cancellation. Product and
identity fields contain only synthetic public values. See
[ADR 0025](../../../../docs/adr/0025-unsigned-dialogue-initialization.md).

## Initialization response-scope fixtures

`initialization-context-v1.json` adds fourteen independent request/response pairs
with explicitly selected issue sets and outcomes. Cases cover identified and
anonymous matching, dialogue-scoped UPD, omitted cached parameters, foreign
references and identities, scoped 3050 updates, opaque advertisements, receipt
without execution and errors accompanied by parameter data.

The .NET suite also checks explicit user/customer separation, missing or invalid
assignments, status ambiguity, envelope restrictions, version-change observations,
512 retained account entries, cancellation and pure repeated evaluation. No
fixture establishes a session or authorizes parameter activation. See
[ADR 0026](../../../../docs/adr/0026-initialization-response-scope.md).

## Initialization attempt traces

`initialization-attempt-v1.json` adds eight independent traces with request and
response bytes, explicit expected user context, event times, transitions, states
and evidence-presence expectations. They cover identified/anonymous execution,
foreign references, missing user context, missing parameters, cancellation,
absolute timeout and stop before start.

The .NET suite also verifies pinned context, caller ownership after terminal
cleanup, replay within an instance, invalid caller input, bound review outcomes,
expiry during comparison, clock regression and concurrent calls. Fake clocks
require no sleeps or bank operations. See
[ADR 0027](../../../../docs/adr/0027-in-memory-initialization-attempt.md).

## Synchronization schema and encoding fixtures

`synchronization-v1.json` adds ten independent unsigned request/response pairs
with explicit mode, input and expected report fields. Cases cover all three modes,
escaped system IDs, maximum message/signature values, separate signature counters,
empty/conflicting shapes, duplicate reports, opaque versions and an error without
a synchronization report. Sixteen-digit counters are stored as decimal strings
in fixture expectations to avoid floating-point loss in tooling.

The .NET suite compares exact request bytes and tests malformed fields, numeric
bounds, references, context separation, anonymous rejection, source retention,
128/129 report limits, cancellation and culture independence. No fixture
qualifies a security profile or applies recovery values. See
[ADR 0028](../../../../docs/adr/0028-synchronization-schemas.md).

## Synchronization context fixtures

`synchronization-context-v1.json` adds fifteen independent request/response pairs
with explicit profile hypotheses, optional prior-dialogue bounds, issue sets and
next-step expectations. Cases cover permitted/prohibited mode combinations,
single/separate signature references, unresolved RAH-9, missing recovery context,
wrong/empty/opaque/duplicate reports, foreign references and excessive reported
message numbers. Matching evidence always requires close and reinitialization.

The .NET suite also checks status ambiguity, invalid assignments, reserved
counter values, envelope restrictions, all 128 reports, source identity and
cancellation. No result authenticates a profile or applies recovered values. See
[ADR 0029](../../../../docs/adr/0029-synchronization-response-context.md).

## Dialogue-end schema and reply fixtures

`dialogue-end-v1.json` adds ten independent unsigned request/response pairs with
explicit independent client/bank counters, issue sets, outcomes and raw code
observations. Cases cover message/scoped closure, escaped Latin-1 identifiers,
maximum counters, explicit abort, missing/conflicting termination, unexpected
data, foreign references and duplicate closing codes across reply levels.

The .NET suite compares exact request bytes and also checks malformed schemas,
context isolation, missing references, ambiguous status, envelope restrictions,
source ownership, culture independence, null handling and cancellation. No
fixture establishes authenticated closure or changes a session. See
[ADR 0030](../../../../docs/adr/0030-dialogue-end-schemas-and-encoding.md).

## Synchronization-and-closing attempt traces

`synchronization-attempt-v1.json` adds twelve independent traces with explicit
requests, replies, profile/recovery context, event times, transitions, states,
counts and stage-evidence expectations. Cases cover all three synchronization
modes, foreign replies at both stages, bound review, abort, cancellation between
stages, the shared deadline, missing recovery context and stop before start.

The .NET suite also verifies exact pinned context, one-time handoff, stage and
counter isolation, terminal reference cleanup, expiry during comparison, clock
regression, concurrency and cancellation/close races. Clocks are synthetic;
no fixture sends a request or applies recovered values. See
[ADR 0031](../../../../docs/adr/0031-in-memory-synchronization-and-closing.md).

## PIN/TAN signature-header fixtures

`pin-tan-signature-header-v1.json` adds ten independent HNSHK-4 wire vectors with
explicit expectations for every exposed field. Cases cover both profiles, procedure
bounds, roles/parties, escaped identifiers, optional timestamps, leap dates, maximum
fields, zero system/reference observations and alternative fillers. Sixteen-digit
references are stored as decimal strings to avoid numeric loss in fixture tooling.

The .NET suite also checks malformed layouts, forbidden binary/optional fields,
unsupported profiles/versions/key types/hash codes, calendar validity, culture
independence, exact ownership, null input and cancellation. No fixture contains a
PIN or TAN or establishes a permitted/authenticated procedure. See
[ADR 0032](../../../../docs/adr/0032-pin-tan-signature-header-schemas.md).

## PIN/TAN signature-context fixtures

`pin-tan-signature-context-v1.json` adds eighteen independent header/request/
procedure-origin vectors with explicit selection, user/control expectations and
issue sets. Cases cover both profiles, zero-system synchronization, missing context,
identity/system/control/procedure mismatches, absent/duplicate permissions,
one-step restrictions, duplicate/future advertisements and foreign/aborted origins.

The .NET suite also checks exact source ownership, separately reparsed response
rejection, missing BPD/UPD, role/version restrictions, advertisement limits,
malformed input, cancellation and culture independence. Matching evidence does
not prove authentication, freshness or permission to send. See
[ADR 0033](../../../../docs/adr/0033-pin-tan-signature-request-context.md).

## PIN/TAN signature-header encoding fixtures

`pin-tan-signature-encoding-v1.json` adds ten independent typed input/wire pairs
covering both profiles, escaped identifiers, segment/function bounds, all timestamp
shapes, maximum fields, zero values and numeric fillers. Optional unused fields
are omitted canonically; the parser fixtures separately preserve alternate empty
spellings. Sixteen-digit references remain decimal strings in JSON.

The .NET suite verifies exact bytes and every input field, malformed values,
fractional-second rejection, source/output ownership, nulls, cancellation and
culture independence. Integration checks retain the stricter request-context
boundary after successful encoding. No fixture contains credentials or produces
a complete signed message. See
[ADR 0034](../../../../docs/adr/0034-pin-tan-signature-header-encoding.md).

## Session credential ownership traces

`credential-buffer-v1.json` adds ten independent local traces containing public
synthetic byte values, kind, lifetime, event times and expected copy/state/count
outcomes. They cover PIN reuse, one-time TAN copy-out, undersized destinations,
cancellation, deadline equality, nonrenewal, clock regression, disposal and opaque
bytes at the local limit. These are buffer traces, not protocol wire fixtures.

The .NET suite additionally verifies actual array zeroization, clearing on failed
capture, span boundaries, copied-prefix cleanup after expiry/cancellation, clock
exceptions, concurrency and fallback finalization. Successful caller copies remain
the caller's cleanup responsibility; no real credentials are used. See
[ADR 0035](../../../../docs/adr/0035-session-credential-buffer-ownership.md).

## PIN/TAN signature-trailer fixtures

`pin-tan-signature-trailer-v1.json` adds twelve independent context/input/wire
vectors with explicit expected results and credential-consumption observations.
Cases cover PIN-only, one-step and variant-1 TAN placement, variant-2 rejection,
synchronization numbering, escaped/maximum values, spaces, invalid octets and
unresolved context. All PIN/TAN fields are public synthetic values.

The .NET suite checks exact bytes, ownership, actual TAN-array zeroization,
undersized output before consumption, incorrect positions/kinds, late cancellation/
expiry cleanup, concurrency and culture independence. Parsing output is test-only;
the production writer never creates a general parser object containing credentials.
See [ADR 0036](../../../../docs/adr/0036-pin-tan-signature-trailer-encoding.md).

## PIN/TAN request-assembly fixtures

`pin-tan-request-assembly-v1.json` adds fourteen independent context/input/wire
vectors for plain initialization and synchronization candidates. They extend the
trailer cases with complete exact framing, body renumbering, an optional empty
certificate and escaped preparation fields. All credentials are public synthetic
values. The Python generator uses its own byte framing and segment operations.

The .NET suite verifies actual size, field preservation, whole-output reserve,
source immutability, TAN ownership, cancellation/expiry across both publication
boundaries, failed-output erasure, culture independence and concurrent TAN use.
General parsing of assembled bytes occurs only with synthetic test data. See
[ADR 0037](../../../../docs/adr/0037-pin-tan-request-assembly.md).

## PIN/TAN request-envelope fixtures

`pin-tan-request-envelope-v1.json` adds seventeen independent wrapped-request
vectors, with exact expected plain and binary-payload bytes. Cases extend the
assembly fixtures with absent/date-only timestamps and escaped envelope identity
fields. Both profiles, system synchronization, TAN placement restrictions and
credential failures are retained. All PIN/TAN values are public synthetic bytes.

The .NET suite verifies complete wire bytes, binary lengths, reserved/logical
numbering, bound fields and fillers, ownership, output reserves, concurrency and
culture independence. Cancellation/expiry exercises all three writer boundaries;
a final clock failure verifies complete failed-output erasure. Only synthetic
test output enters general parsers, and response-envelope parsing remains
restricted to bank-side bodies. See
[ADR 0038](../../../../docs/adr/0038-pin-tan-request-envelope-assembly.md).

## Assembled PIN/TAN request-reference fixtures

`pin-tan-request-binding-v1.json` adds twenty-three independent response/context
vectors with exact expected issues and referenced request roles. Cases cover both
profiles, initialization/synchronization numbering, missing/old/foreign references,
signature/wrapper errors, profile and outer-reference mismatches, unknown data,
future versions and correctly scoped errors or foreign parameter identities.

The .NET suite verifies original source instances, immutable metadata, actual
envelope-writer geometry, binding after credential/output erasure, cancellation,
fixed diagnostics and repeated/concurrent pure comparison. Fixtures contain no
real bank data or credentials. Matching references deliberately do not imply
execution or validate returned parameter identities. See
[ADR 0039](../../../../docs/adr/0039-pin-tan-assembled-request-reference-binding.md).

## Assembled PIN/TAN initialization semantics fixtures

`pin-tan-initialization-evidence-v1.json` adds thirty independent wrapped response/
context vectors with expected top-level and detailed parameter issues and nullable
version-change observations. Cases cover both profiles, correct/foreign identities,
missing data, scoped updates, UPD zero, execution/receipt/pending/error reports,
signature versus identification roles and envelope scope. Values are synthetic.

The .NET suite adds provenance checks for byte-identical foreign response instances,
wrong request kind, cancellation, fixed diagnostics and concurrent pure evaluation.
It verifies that previously matching references with a foreign returned user now
require semantic review, while the unsigned comparator retains its restrictions.
See [ADR 0040](../../../../docs/adr/0040-pin-tan-initialization-response-semantics.md).

## Assembled PIN/TAN synchronization semantics fixtures

`pin-tan-synchronization-evidence-v1.json` adds thirty-three independent context/
response vectors with top-level and detailed synchronization issues. Both profiles,
system assignment, message recovery, exact counter bounds, missing/duplicate/future
reports, status roles, envelope identities and uninterpreted parameters are covered.
All identities and report values are public synthetic data.

The .NET suite verifies exact source retention, wrong-kind/source rejection,
mandatory close/reinitialize next steps, null/cancellation/concurrency behavior and
message-recovery envelope-writer integration. Shared report-check extraction retains
all unsigned behavior. See
[ADR 0041](../../../../docs/adr/0041-pin-tan-synchronization-response-semantics.md).
